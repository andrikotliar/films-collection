{
  "name": "films-collection",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm run build:packages && pnpm run build:apps",
    "build:apps": "pnpm -r --filter \"./apps/*\" run build",
    "build:packages": "pnpm -r --filter \"./packages/*\" run build",
    "build:api": "pnpm run build:packages && pnpm --filter @films-collection/api build",
    "db:start": "docker compose up database -d",
    "db:init": "pnpm --filter @films-collection/api run db:push && pnpm --filter @films-collection/api run db:seed",
    "db:init:empty": "pnpm --filter @films-collection/api run db:push",
    "dev": "pnpm run -r --parallel --aggregate-output dev"
  },
  "devDependencies": {
    "@eslint/js": "^9.36.0",
    "eslint": "^9.36.0",
    "typescript-eslint": "^8.44.0"
  },
  "author": "Andrii Kotliar",
  "repository": {
    "type": "git",
    "url": "https://github.com/andrikotliar/films-collection"
  }
}