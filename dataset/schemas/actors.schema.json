{
  "title": "Actors",
  "type": "array",
  "description": "List of available actors",
  "items": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "Unique identifier (hexadecimal string) of an actor"
      },
      "name": {
        "type": "string",
        "description": "Full name of an actor"
      },
      "image": {
        "type": "string",
        "description": "An actor's photo. It contains the file name and the file extension."
      }
    },
    "required": [
      "id",
      "name",
      "image"
    ]
  }
}