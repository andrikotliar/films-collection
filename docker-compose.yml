services:
  database:
    image: postgres:17-bookworm
    restart: unless-stopped
    ports:
      - "54321:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    env_file:
      - ./server/.env

volumes:
  pgdata: